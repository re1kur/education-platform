spring:
  application:
    name: file-service
  docker:
    compose:
      enabled: true
      file: file-service/docker/docker-compose.yml

minio:
  access-key: developer
  secret-key: developer
  endpoint: http://localhost:9000
  endpoint-override: http://localhost:9000
  url:
    time-to-live: 7
  default-bucket: developer